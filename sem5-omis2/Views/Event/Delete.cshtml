@model sem5_omis2.Models.Event

@{
    ViewData["Title"] = "Удаление мероприятия";
}

<h2>Удаление мероприятия</h2>

<div class="alert alert-danger">
    Вы уверены, что хотите удалить мероприятие **@Model.Name**? Это действие необратимо.
</div>

<form asp-action="DeleteConfirmed" method="post">
    <input type="hidden" asp-for="Id" />

    <button type="submit" class="btn btn-danger">Удалить</button>
    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-secondary">Отмена</a>
</form>
